#include <iostream>

void swapValues(char arr[], int a, int b)
{
    char temp = arr[a];
    arr[a] = arr[b];
    arr[b] = temp;
}

void printComb(char arr[])
{
}

void genComb(char arr[], int i = 0)
{
    if (i < 4)
    {
    }
}

int main()
{
    char arr[] = {'A', 'B', 'C', 'D'};
    genComb(arr);
}